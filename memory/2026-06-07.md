# Session Memory — 2026-06-07

## Kill Switch Bug Fix (08:38 HKT)
- **Bug:** Transient Hyperliquid 502 returned $0.00 balance → permanent kill switch. Bot dead ~40h.
- **Root cause #1:** No retry on balance = 0.0. One bad API call = indefinite kill.
- **Root cause #2:** kill_switched=True had no kill_reason attribution on balance path.
- **Root cause #3:** execution_monitor's detect_tpsl_rejection_rate fired EVERY cycle on kill_switched=True even for non-TPSL reasons.
- **Fix (3 parts):**
  1. Retry-on-zero: if balance < MIN_BALANCE, sleep 3s, retry once. Transient 502 recovers.
  2. kill_reason attribution plus stale-recovery: if balance-reasoned kill is stale, fresh balance check auto-clears.
  3. Renamed detect_tpsl_rejection_rate → detect_persistent_kill_switch, added dedup suppression.
- **CEO directive:** Wosobu approved revised spec (retry pattern), preferred over self-heal.
- **7-step deployment checklist:** ALL STEPS followed. Verified. Backup created.
- **UAT:** 62/62 pass. Integration tests pass.

## Project Aqua Rename (15:13 HKT)
- **Context:** The codebase previously had `framework_v2/` as the directory name. Wosobu's dev bay handoff called it "Project Aqua" and said no "v2" branding.
- **Action:** Renamed directory `framework_v2/` → `aqua/`. Updated ALL imports (`framework_v2.strategies.*` → `aqua.strategies.*`), log paths, docstrings, ARCHITECTURE.md. Orchestrator now says `[Aqua]`, status says `project: "aqua"`.
- **Confirmed:** grep found zero stale references.

## Project Aqua Phase 2 (15:08 HKT)
- **Action:** Ported BB Core from strategies/bb_core.py → aqua/strategies/bb_core.py.
- **Contract:** check_signal(coin, df_1h, current_price=0.0, **kwargs) → Optional[Dict]. Native types, no score path.
- **Testing:** 9/9 protocol checks pass.

## Project Aqua Phase 3 (15:42 HKT)
- **Action:** Ported VolSurge + FundingProxy from legacy → aqua/strategies/.
- **Testing:** 20/20 contract tests pass. All 4 strategies now registered in orchestrator (turtle_soup, bb_core, vol_surge, funding_proxy).

## Project Aqua Phase 4 (21:58 HKT)
✅ **5 strategies ported**: turtle_soup, bb_core, vol_surge, funding_proxy, btc_stat_arb
✅ **3 filters built in aqua/core/filters/**: oi_gate, orderflow_filters, vwap_variance
✅ **Risk Manager**: kill switch, cooldown, max positions, position sizing
✅ **Order Queue**: filter chain → risk → execution pipeline
✅ **Bug fixed**: position_size double-division by entry_price (returning 0.04 instead of 4.0)

**Remaining for Phase 5:** Live switchover — wire HL connector into order_queue, point cron at orchestrator.

## Afternoon/Evening Heartbeat Actions
- **P1 Revenue (AI Directory)**: Affiliate disclosure page (FTC-compliant, 6 sections, all 16 programs). Published "AI Market Research SEA 2026" (24K words, FAQ schema). Restored overwritten blog page + saved CEO draft as new slug.
- **P3 Strategic (LifeOS)**: LifeDashboard component (cross-category stats, activity feed, personality summary). Cloud session sync on plugin open (Supabase POST /api/session). Cloud message sync on chat (3 code paths).
- **P4 Strategic (Titan)**: Stripe Checkout API route (real/mock fallback). Pricing page wired to API. Preloaded Geist fonts. `SoftwareApplication` + `BreadcrumbList` JSON-LD. FAQPage structured data to /categories pages.
- **P5 Strategic (AI Directory)**: FAQPage structured data to 13 category pages. Sitemap fix for affiliate-disclosure.
- **Security**: DeepSeek API key rotation received, 6 scripts fixed from hardcoded to env vars. AGENTS.md SEKRETZ rule hardened.

## Cron Health (final, 23:26 HKT)
- 22 crons, 17 ok, 5 errored (all known/transient):
  - **rd-fast-loop-2h**: "Request was aborted" — DeepSeek latency spike
  - **reverse-engineer-6h**: Timeout — self-recovers
  - **omnimind-consolidation-backup**: Tilde path — fix applied, self-resolves Jun 8 03:00
  - **trading-pulse-30min**: self-recovered ✅
- ceo-24-7-work-engine self-recovered ✅

## Key CEO Directives (captured today)
- "Project Aqua, not framework_v2" — no v2 branding, ever
- Follow 7-step deployment checklist for every production change
- Prefer direct fixes over recovery code (retry-on-zero > self-heal)
- Meet CEO with critical thinking, not blind implementation

## Blockers (unchanged)
- Supabase DNS (LifeOS cloud sync)
- Revenue keys (Stripe, affiliate API tokens)
- Titan domain alias config
- Vercel deploy keys

## Pending
- Project Aqua Phase 5: Live switchover
- Draft blog post ready at drafts/blog-ai-customer-support-chatbots-comparison-2026.json
