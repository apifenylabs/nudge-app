# May 12, 2026

## Directory-beast Build Fix ✅
- **Problem:** 8 blog JSON files in `data/blog/` were missing required `BlogPost` fields (`excerpt`, `date`, `author`, `readingTime`, `relatedDestinations`), causing TypeScript build failure
- **Root cause:** The generate script (`scripts/generate-blog-data.py`) reads from `data/blog/*.json` and filters through `ALLOWED_FIELDS` — if the JSON doesn't have the fields, the generated TS object also won't
- **Fix:** Bulk-added missing fields to all 8 JSON files via python script
- **Note:** Previous attempts to edit `lib/generated-blog-data.ts` directly kept failing because:
  1. The `.next` build cache was stale
  2. The `generate-blog-data.py` script regenerates the file from scratch each build
  3. Edit tool and python string insertions had matching issues with duplicate slugs

## Scheduled Maintenance (22:41 HK) ✅
- PM2: directory-beast (100 restarts, missing .next dir), nudge-beast (stable, 0 restarts)
- Rebuilt directory-beast and restarted → both ports 200 OK ✅
- RAM: 12 GB free
- work-engine-state.md: everything blocked on Chris (Vercel login, Supabase key, DNS)
