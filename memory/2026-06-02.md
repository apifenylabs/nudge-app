# Memory — June 2, 2026

## Live Execution Day — The $981 Battle

### Key Events
- **07:31 HKT**: Wosobu demanded go-live. I had left bot in paper mode overnight due to misinterpreting conditional ("go paper if micro trades" = conditional, not blanket).
- **07:34 HKT**: Wosobu corrected me — $981 balance, not $1,135. Three bugs found and patched in morning triage:
  1. **TimeSyncGuard blocking live mode** — reading nonexistent `unixTime` field from HL meta endpoint (garbage year 58387). Fixed to NTP/HTTP Date header + widened tolerance to 1s.
  2. **Unified account blindness** — USDC in spot pool, not perp margin, so `user_state()` returned $0. Fixed `get_account_state()` to cross-check portfolio endpoint.
  3. **Stale balance bridge** — `bot_state.json` still had $1,135 from May 31.
- **08:00 HKT**: Morning Pulse fixed. All signals neutral (range-bound market).
- **09:52 HKT**: Found missing 30-min trading pulse cron — I had deleted it during kill switch actions. Recreated.
- **10:44 HKT**: Flipped back to paper mode by Wosobu's order. $956 at switch time.
- **12:19 HKT**: Diagnosed the real bug — ghost killer closes SOL VolSurge longs every minute. Loop: VolSurge opens → TP/SL attach fails silently ("Main order cannot be trigger order") → next cron cycle sees position not in state.json → market close → reopen. Burned $30 in slippage.
- **Astronomer isolation**: Locked to 0% allocation as instructed. Removed from live config.

### Hard Rules Captured
1. Bot is LIVE unless told otherwise — "go paper if micro trades" is a conditional, not a directive
2. Do not delete crons during kill switch — pause instead
3. The ghost killer loop on SOL is the #1 bug blocking profitability
4. Paper mode first if unsure about behavior

### CPSO Fixes Applied (13:34 HKT)
1. **Grouped Entry+TP/SL**: Changed `place_tpsl_group` from two separate calls (market_open + bulk_orders) into a single `bulk_orders([entry, tp, sl], grouping='normalTpsl')`. HL requires all 3 in one transaction for grouping to work.
2. **Atomic State Registration**: State written BEFORE exchange call with STATUS="OPEN_UNPROTECTED". If TP/SL fails, the ghost killer finds the position tracked and doesn't market-close it. Emergency close in `place_tpsl_group` is the ONLY close path. `result.success=False` on emergency close prevents caller from writing stale state.
3. **Churn Rate Limiter**: Auto-detects 3+ positions in 3min per strategy → 2h hard lockout. Implemented as pre-gate in `place_signal_entry` and as `run_once` monitoring in `churn_lockouts` dict in state.json.

### Full State Machine Audit (12:47 HKT)
Completed a full linear audit of every execution path through place_signal_entry + place_tpsl_group:

**Paths identified and fixed:**

**Path A — Entry rejected**: State written but HL rejects entry (insufficient margin, etc.). Old code left stale OPEN_UNPROTECTED in state forever. **FIXED**: Failure cleanup now removes placeholder from state and active_trades.json.

**Path B — Entry fills, SL fails**: Emergency close fires successfully. Old code left stale OPEN_UNPROTECTED. Ghost killer skips it because tracked in open_symbols. Position is gone on HL. **FIXED**: Same cleanup — placeholder removed on any failure.

**Path C — Grouped order timeout/unexpected format**: bulk_orders returns non-dict or empty statuses. Falls through to "Unknown failure". **FIXED**: Cleanup removes placeholder. Position status unknown (partial fill possible with IoC) — next cycle's ghost killer scans HL assetPositions and will close if untracked.

**Path D — Emergency close ALSO fails**: Worst case — live unhedged position on HL with no way to close it. **FIXED**: Kill switch is automatically triggered when error contains "CRITICAL" or "EMERGENCY CLOSE ALSO FAILED". Bot stops all trading immediately.

**Active trades.json cleanup**: register_trade() is called BEFORE exchange call (matching atomic state pattern). On failure, close_trade() cleans up the registration so post-restart protection doesn't preserve a trade that never existed.
